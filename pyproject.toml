[project]
name = "pomodora-clock"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.0.0",
    "pygame>=2.6.1",
    "ttkbootstrap>=1.20.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.17.0",
]
